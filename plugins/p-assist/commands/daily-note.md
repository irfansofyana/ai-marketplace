---
description: Create a daily note in Capacities
argument-hint: [note content]
---

# Create Daily Note in Capacities

Create a daily note in Capacities with the following content:

"$ARGUMENTS"

**Instructions:**

1. Format the note content appropriately:
   - Use proper markdown formatting
   - Add relevant tags using #hashtag format
   - Include timestamps if needed
   - Structure with bullet points or headings as appropriate

2. Use `mcp__plugin_p-assist_n8n_pa__capacities_daily_note` to create the entry:
   - Pass the formatted content as `parameters1_Value`
   - Supports markdown syntax

3. Confirm the entry was created successfully

**Output Format:**

```
Daily note created successfully in Capacities

**Content Preview:**
[First few lines of the note]

**Tips:**
- View your note in Capacities to add more context or links
```

**Content Enhancement:**
- Automatically add today's date if not specified
- Suggest relevant tags based on content
- Format lists and structure for readability
